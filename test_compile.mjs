import solc from "solc"; const source = `pragma solidity ^0.8.0; contract SimpleStorage { uint256 private _value; constructor() { _value = 42; } }`; const input = { language: "Solidity", sources: { "contract.sol": { content: source } }, settings: { outputSelection: { "*": { "*": ["abi", "evm.bytecode"] } } } }; const out = JSON.parse(solc.compile(JSON.stringify(input))); console.log(JSON.stringify(out, null, 2));
